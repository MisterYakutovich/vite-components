/*import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';


export const countryApi = createApi({
  reducerPath: 'api',
  baseQuery: fetchBaseQuery({ baseUrl: 'https://moyaposylka.ru/api/v1/countries' }),
  endpoints: (builder) => ({
    getData: builder.query({
      query: () => ``,
    }),
  }),
});

export const { useGetDataQuery } = countryApi;
export const { getData } = countryApi.endpoints;*/
