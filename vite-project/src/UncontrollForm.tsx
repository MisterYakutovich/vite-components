//import { useDispatch, useSelector } from "react-redux";
//import { updatePhoto } from "./redux/slices/formslice";
//import { RootState } from "./redux/store";

function UncontrollForm() {
  /* const dispatch = useDispatch();
    const handleImageChange = (event: React.ChangeEvent<HTMLInputElement>) => {
        const file = event.target.files![0];
        const reader = new FileReader();
        reader.onload = (e) => {
          const base64Image = e.target!.result as string;;
          dispatch(updatePhoto(base64Image)); // Вызываем action creator для обновления состояния хранилища с данными изображения
        };
        reader.readAsDataURL(file);
        
      };
      const formData = useSelector((state: RootState) => state.form);
      console.log(formData)*/
  return <div>ldtyhup</div>;
}
export default UncontrollForm;
