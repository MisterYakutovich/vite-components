/*import '@testing-library/jest-dom';
import { fireEvent, render,screen } from "@testing-library/react";


import Paginations from './Paginations';





test("render learn react input",()=>{
    render(<Paginations  />)
   const input =  screen.getByPlaceholderText("Number of cards per page")
   expect(screen.queryByTestId("container")).toContainHTML("")
   fireEvent.input(input,{
    target:{value:"50"}
   })
   expect(screen.queryByTestId("container")).toContainHTML("5")
  
   
    
 
    screen.debug()
})*/
